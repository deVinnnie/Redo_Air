package com.realdolmen.course.domain;

import com.realdolmen.course.utilities.persistence.JpaPersistenceTest;
import org.junit.Test;

import java.util.Calendar;

public class FlightTest extends JpaPersistenceTest{

    /*@Test
    public void testCreateFlight(){
        Airport departureAirport = entityManager().find(Airport.class, 200L);
        Airport arrivalAirport = entityManager().find(Airport.class, 300L);

        Plane plane = entityManager().find(Plane.class, 50);
        Flight flight = entityManager().find(Flight.class, 200L);

        flight.setArrival(arrivalAirport);
        flight.setDeparture(departureAirport);
        flight.setPlane(plane);
    }*/
}
